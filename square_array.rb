def old_square_array(array)
  squared_array = []
  for each in array do
    squared = each * each
    squared_array << squared
  end
  return squared_array
end

def square_array(array)
  array.each do
end